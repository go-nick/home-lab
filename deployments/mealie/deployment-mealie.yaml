apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp: null
  labels:
    app: deploy-mealie
  name: deploy-mealie
  namespace: mealie
spec:
  replicas: 1
  selector:
    matchLabels:
      app: deploy-mealie
  template:
    metadata:
      labels:
        app: deploy-mealie
    spec:
      containers:
      - image: ghcr.io/mealie-recipes/mealie:v3.20.1
        name: mealie
        ports:
          - containerPort: 9000 
        volumeMounts:
          - mountPath: /app/data
            name: mealie-pv # must match name of the volume
      volumes:
        - name: mealie-pv # name of the volume
          persistentVolumeClaim:
            claimName: mealie-storage # name of the persistentVolumeClaim



